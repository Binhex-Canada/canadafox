diff --git a/browser/app/distribution/distribution.ini b/browser/app/distribution/distribution.ini
index 5c16c3bab1..5c5a0620b6 100644
--- a/browser/app/distribution/distribution.ini
+++ b/browser/app/distribution/distribution.ini
@@ -1,4 +1,38 @@
 [Global]
-id=mozilla-official
+id=canadafox
 version=1.0
-about=Mozilla Firefox Official Build
\ No newline at end of file
+about=CanadaFox
+
+[BookmarksToolbar]
+item.1.type=folder
+item.1.title=Canadian Services
+item.1.folderId=1
+
+[BookmarksFolder-1]
+item.1.type=bookmark
+item.1.title=Canada Revenue Agency (CRA)
+item.1.link=https://www.canada.ca/en/revenue-agency.html
+
+item.2.type=bookmark
+item.2.title=Service Canada
+item.2.link=https://www.canada.ca/en/employment-social-development/corporate/portfolio/service-canada.html
+
+item.3.type=bookmark
+item.3.title=Canada.ca — Health
+item.3.link=https://www.canada.ca/en/services/health.html
+
+item.4.type=bookmark
+item.4.title=CBC News
+item.4.link=https://www.cbc.ca/news
+
+item.5.type=bookmark
+item.5.title=Canada Benefits Finder
+item.5.link=https://www.canada.ca/en/services/benefits/finder.html
+
+item.6.type=bookmark
+item.6.title=BC Services A-Z
+item.6.link=https://www2.gov.bc.ca/gov/content/home/services-a-z
+
+item.7.type=bookmark
+item.7.title=WelcomeBC — After Arriving in B.C.
+item.7.link=https://www.welcomebc.ca/start-your-life-in-b-c/after-arriving-in-bc
diff --git a/browser/app/distribution/moz.build b/browser/app/distribution/moz.build
index 0a175ac0a4..7e30f4c6bc 100644
--- a/browser/app/distribution/moz.build
+++ b/browser/app/distribution/moz.build
@@ -9,6 +9,14 @@ if CONFIG["BUILT_BY_MOZILLA"]:
         "distribution.ini",
     ]
 
+# CanadaFox: distribution.ini is normally only packaged for official
+# Mozilla builds (see the BUILT_BY_MOZILLA check above), but we use it to
+# seed a pre-loaded bookmarks folder of Canadian services, so ship it
+# unconditionally.
+FINAL_TARGET_FILES.distribution += [
+    "distribution.ini",
+]
+
 # CanadaFox: ship an enterprise-policy file so the default search
 # engine is set the same way distributors do it (durable across the
 # remote-settings-driven, locale/region search config), rather than a plain
diff --git a/browser/components/BrowserGlue.sys.mjs b/browser/components/BrowserGlue.sys.mjs
index 8d21b1fea6..39ed80ecb7 100644
--- a/browser/components/BrowserGlue.sys.mjs
+++ b/browser/components/BrowserGlue.sys.mjs
@@ -404,6 +404,7 @@ BrowserGlue.prototype = {
 
     this._initCiraDnsToggle();
     this._initKeywordShortcuts();
+    this._ensureCanadianServicesBookmarks();
 
     // handle any UI migration
     this._migrateUI();
@@ -477,6 +478,98 @@ BrowserGlue.prototype = {
     }
   },
 
+  // CanadaFox: keep the "Canadian Services" bookmarks folder topped up with
+  // every entry we ship, on every startup. distribution.ini's bookmark
+  // import (see browser/app/distribution/distribution.ini) only ever runs
+  // once per profile, so any entry added in a later CanadaFox version would
+  // never reach a profile that already existed -- confirmed missing entries
+  // on a real, already-used profile after adding two more here. Checking by
+  // URL and inserting only what's missing means this is safe to run every
+  // launch, and doesn't touch anything the user has since moved or renamed.
+  CANADAFOX_BOOKMARKS_FOLDER_TITLE: "Canadian Services",
+  CANADAFOX_BOOKMARKS: [
+    {
+      title: "Canada Revenue Agency (CRA)",
+      url: "https://www.canada.ca/en/revenue-agency.html",
+    },
+    {
+      title: "Service Canada",
+      url: "https://www.canada.ca/en/employment-social-development/corporate/portfolio/service-canada.html",
+    },
+    {
+      title: "Canada.ca — Health",
+      url: "https://www.canada.ca/en/services/health.html",
+    },
+    { title: "CBC News", url: "https://www.cbc.ca/news" },
+    {
+      title: "Canada Benefits Finder",
+      url: "https://www.canada.ca/en/services/benefits/finder.html",
+    },
+    {
+      title: "BC Services A-Z",
+      url: "https://www2.gov.bc.ca/gov/content/home/services-a-z",
+    },
+    {
+      title: "WelcomeBC — After Arriving in B.C.",
+      url: "https://www.welcomebc.ca/start-your-life-in-b-c/after-arriving-in-bc",
+    },
+  ],
+
+  async _ensureCanadianServicesBookmarks() {
+    // distribution.ini's own bookmark import runs later, asynchronously,
+    // from a separate startup path (PlacesBrowserStartup.sys.mjs) that
+    // isn't awaited here. On a genuinely new profile it's the authoritative
+    // (and race-free, since it owns folder creation) way these bookmarks
+    // get created, so defer to it entirely rather than risk this code and
+    // that import both deciding the folder doesn't exist yet and each
+    // creating one. Mirrors distribution.sys.mjs's own newProfile check.
+    if (!Services.prefs.prefHasUserValue("browser.migration.version")) {
+      return;
+    }
+    try {
+      let db = await lazy.PlacesUtils.promiseDBConnection();
+      let rows = await db.executeCached(
+        `SELECT b.guid FROM moz_bookmarks b
+         JOIN moz_bookmarks p ON b.parent = p.id
+         WHERE p.guid = :toolbarGuid AND b.title = :title AND b.type = :folderType
+         LIMIT 1`,
+        {
+          toolbarGuid: lazy.PlacesUtils.bookmarks.toolbarGuid,
+          title: this.CANADAFOX_BOOKMARKS_FOLDER_TITLE,
+          folderType: lazy.PlacesUtils.bookmarks.TYPE_FOLDER,
+        }
+      );
+
+      let folderGuid;
+      if (rows.length) {
+        folderGuid = rows[0].getResultByName("guid");
+      } else {
+        let folder = await lazy.PlacesUtils.bookmarks.insert({
+          type: lazy.PlacesUtils.bookmarks.TYPE_FOLDER,
+          parentGuid: lazy.PlacesUtils.bookmarks.toolbarGuid,
+          title: this.CANADAFOX_BOOKMARKS_FOLDER_TITLE,
+        });
+        folderGuid = folder.guid;
+      }
+
+      for (let { title, url } of this.CANADAFOX_BOOKMARKS) {
+        if (await lazy.PlacesUtils.bookmarks.fetch({ url })) {
+          continue;
+        }
+        await lazy.PlacesUtils.bookmarks.insert({
+          parentGuid: folderGuid,
+          title,
+          url,
+        });
+      }
+    } catch (ex) {
+      console.error(
+        "CanadaFox: failed to ensure Canadian Services bookmarks",
+        ex
+      );
+    }
+  },
+
   _checkForOldBuildUpdates() {
     // check for update if our build is old
     if (
